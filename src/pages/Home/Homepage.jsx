import React from 'react';
import SearchBar from '../../components/Home/SearchBar';

const Homepage = () => {
    return (
        <div>
            <SearchBar></SearchBar>
        </div>
    );
};

export default Homepage;